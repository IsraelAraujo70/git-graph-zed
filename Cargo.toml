[package]
name = "git-graph"
version = "0.1.0"
edition = "2021"
description = "Git history visualization helpers for the Zed editor."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.1.0"
